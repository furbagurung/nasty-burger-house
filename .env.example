# Secure HTTPS endpoint that receives validated pay-at-pickup orders.
ORDER_WEBHOOK_URL=https://example.com/nasty-burger-orders

# Optional bearer token shared with the order receiver.
ORDER_WEBHOOK_SECRET=
